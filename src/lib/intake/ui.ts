// Shared input/label styling for the intake surfaces (quick form, chat panel,
// review panel) so the three keep one visual language without importing from
// each other.
export const INPUT_BASE =
  'w-full min-h-[44px] rounded-xl border border-border-subtle bg-white px-4 py-3 text-base text-ink placeholder:text-ink-faint transition-colors focus:border-traq-purple focus:outline-none focus:ring-0 sm:text-sm';

export const LABEL_BASE = 'text-[11px] font-semibold uppercase tracking-widest text-ink-faint';
